<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/layoutAddOptions"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            android:text="Add Product"
            android:textSize="24sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnScanBarcode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/icon_barcode"
            android:text="Scan Barcode" />

        <Button
            android:id="@+id/btnAddManually"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:drawableLeft="@drawable/icon_add"
            android:text="Add Manually" />

    </LinearLayout>

    <include
        android:id="@+id/layoutAddManually"
        layout="@layout/layout_add_manually" />

    <include
        android:id="@+id/layoutAddMasterProduct"
        layout="@layout/layout_add_master_product"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</FrameLayout>